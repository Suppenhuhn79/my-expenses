/**
 * @callback ChoiceCallback
 * Callback function type for `choices`.
 * @param {string} choiceKey Key of chosen item.
 * @param {boolean} interactive `true` if the choice was set by an (user) event, otherwise `false`.
 *
 * @callback vikbCallback
 * Callback function type for `vikb` virtual keyboard detector.
 * @param {"opened"|"closed"} event `"opened"` if virtual keyboard just popped on, or `"closed"` if it popped off.
 */
